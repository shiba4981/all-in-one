// Page-specific JS for Event Management
// This file can be used to initialize page-only widgets or override behaviors

(function(){
  document.addEventListener('DOMContentLoaded', () => {
    // Example: any small per-page initializations can go here
    const modal = document.getElementById('portfolioModal');
    if(modal){
      // Let root script handle the modal; this is a hook for future per-page logic
    }
  });
})();
